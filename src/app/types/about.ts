export interface AboutContent {
  title: string;
  introduction: string;
}

export interface CareerContent {
  company: string;
  meta: string;
  period: string;
  description: string;
  techStackText: string;
}
